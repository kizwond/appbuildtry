const SectionForResult = ({ title, content }) => (
  <section className="w-full">
    <h2 className="ForMobilePageMainTitle">{title}</h2>
    <div className="px-[4px]">{content}</div>
  </section>
);

export default SectionForResult;
